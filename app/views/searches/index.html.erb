<h1> Поиск билетов <h1>
<%= render 'form_stations' %>

<p>
<% if @trains.present? %>

  <table style="widtd:60%">
  <tdead>
    <tr>
      <td> Поезд </td>
      <td> Отравление </td>
      <td> Прибытие </td>
      <td> Покупка </td>
    </tr>
  </tdead>
  <% @trains.each do |train| %>
  <tbody>
    <tr>
      <td> <%= train.number %> </td>
      <td> <%= train.route.railway_stations_routes.first.arrival_time %> </td>
      <td> <%= train.route.railway_stations_routes.first.departure_time%> </td>
      <td> <%= link_to 'Купить билет', new_ticket_path(train_id: train.id, start_station_id: @start_station.id, end_station_id: @end_station.id) %> </td>
    </tr>
  </tbody>
    <% end %>
  </table>
<% end %>

<%%>
